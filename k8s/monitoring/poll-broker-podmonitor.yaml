apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: poll-broker
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: poll-broker
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
